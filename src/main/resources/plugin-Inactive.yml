name: ${project.artifactId}
main: net.pocketpixels.hubmanager.BukkitManger
version: ${project.version}
website: ${project.url}
description: A hub manager for Pocket Pixels
author: Dallen
commands:
    createfall:
        description: set an object to fall
        usage: |
                /createfall
